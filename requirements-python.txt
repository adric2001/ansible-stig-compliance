# Python deps for the amazon.aws.aws_ec2 inventory plugin (Ansible controller).
# Install into the same Python Ansible uses, e.g.:
#   $(dirname $(which ansible))/python -m pip install -r requirements-python.txt
# or with Homebrew Ansible:
#   /opt/homebrew/Cellar/ansible/*/libexec/bin/python -m pip install -r requirements-python.txt
boto3>=1.34
botocore[crt]>=1.34
